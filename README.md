# localai-wsl-client
cd /mnt/c/localai-wsl-client && chmod +x gh0stat_dashboard.sh && ./gh0stat_dashboard.sh
localai-wsl-client/
├── gh0stat_public/         ← Cloned minimal (your shadow, BSD-2-Clause)
├── models/optimized/       ← 1B–3B Q4_K_M (bench-proven)
├── results/                ← public-baseline.txt + optimized.txt + tooltest.txt
├── sovietai/               ← Symlinked vault (configs/models/etc.)
├── DASHBOARD.tsx           ← Lean from public (or fallback)
└── logs/                   ← Cascade output
iNTUITIV.ascend
